# Interní / strategické dokumenty — drží se LOKÁLNĚ, nikdy ne na (veřejný) GitHub
STRATEGY.md
BUSINESS.md
PORTFOLIO.md

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
automation/data/drift-report.md
automation/data/.llm-cache/
__pycache__/
scripts/.alerts-sent*.json

# Secrets — nikdy do repa
*.env
.env
engine/.env
.env.local

# Claude Code — lokální override (env allow-flag pro guard.mjs) zůstává lokálně,
# NIKDY se necommitne → cloud routine ho nedostane (= zůstane v block režimu)
.claude/settings.local.json

# lokální výstup marketing konektorů (analytika — NIKDY na veřejný web)
_marketing-out/
